
export const RPC_URL = "https://polygon.llamarpc.com";

export const TG_CHAT_ID: number = 0;

export const TG_TOKEN: string = '';

export const MIN_WAIT_TIME = 0; // in minutes

export const MAX_WAIT_TIME = 0; // in minutes

export const MIN_TOKEN_BALANCE = 2; // in USD

export const MAX_VOLUME_NEEDED = false; // true if you want to use get MAX_VOLUME

export const MAX_VOLUME = 10_000;

export const MAX_TX_SINGLE = 20;

export const RND_AMOUNT = false; // true if you want to use random amount

export const MIN_AMOUNT_PERCENT = 20; // % of the token balance

export const MAX_AMOUNT_PERCENT = 100; // % of the token balance

// export const MAX_TX_MULTI = 0;

// export const IS_HTTPS_PROXY = true; // true if you want to use http proxies